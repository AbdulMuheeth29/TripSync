# Test Environment Variables
NODE_ENV=test
PORT=3001
HOST=localhost

# Use in-memory storage for tests (or a test database)
# DATABASE_URL=postgresql://test:test@localhost:5432/tripsync_test

# Test JWT secret (must be 32+ chars)
JWT_SECRET=test-jwt-secret-min-32-characters-long-for-testing

# Disable external services in tests
# AI_INTEGRATIONS_ANTHROPIC_API_KEY=test-key
# STRIPE_SECRET_KEY=sk_test_...
